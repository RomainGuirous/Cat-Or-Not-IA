# Central configuration for image processing and paths
# Modify IMG_SIZE here to propagate the change across the project

IMG_SIZE = (128, 128)  # (width, height) or (H, W) consistent with pil resize usage
